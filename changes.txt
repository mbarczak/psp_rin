より正確な変更点

v1.32
・終了前の保存処理がまだ終わっていないのに終了されてしまい、
　設定ファイルやセーブファイルが0byteになるという問題を修正
・ローバッテリ時に強制スリープ
・変更がなかったときはSRAM保存しない（SRAMのCRC32を比較）
・メニュー中は常時バッテリーライフ表示

v1.31
・mbc_write関数を関数ポインタに変更。パフォーマンスは特に改善されず。
・zlibの関数でgz圧縮するとファイルアクセスが何度も発生して遅くなるため、
　自前の関数に書き換え。
・メモリ上へのステートセーブに対応
・バッテリ残量警告
・□ボタンでクイックスロット後退

v1.3
・チート機能を追加。cpu_read関数をトラップするとチート不使用時でも
　体感差が出るほど遅くなったので、cpu_write関数をトラップする仕様にした。
　そのためにTGB Dual拡張の比較系のコードは実装できず。
・apuを少し効率化。チート機能で重くなった分を合わせてトントン。
・GPU描画の叩き台をuosnes9xを参考に作成
　（しかしGBだとボケボケになるなぁ…）

v1.29
・apuを効率化
・サウンドバッファ関連の変更
・ステートセーブ・ロードで前回使ったスロットを記憶するようにした
・ステートファイルのないスロットからはロード不可にした

v1.28
・freadとfwriteをライブラリも含めて全て不使用にした。
　この変更に伴いlibcは標準のものに戻した。
・前回開いたフォルダの記憶に失敗していたバグを修正
・GZIPのROM読み込みに対応。展開速度は当環境ではZIPの5倍ほど。
・remove()を使う前にファイルの存在確認を行うようにした。
・メニューを抜ける際にサウンドバッファをクリア

v1.27
・コンパイラを謎物体さんのところのgcc 4.0.0に変更。
・libc組み込み。謎物体さんのnewlibを使用。
　ただしそのままではfwriteが激遅だったので、
　stdio.hのBUF_SIZを32KBに増やしてコンパイルし直した。
・zlib組み込み。makefileだけ弄ってそのままコンパイル。
・ruka氏のunziplibをzlib向けに再構築。
・libpng組み込み。makefileだけ弄ってそのままコンパイル。
・pngの壁紙に対応
・サムネイルのフォーマットをPNGに変更
・SRAMファイルとステートファイルのGZIP圧縮
・ステートセーブの高速化
・ステートスロットの最大値を9に変更
・SGBの描画を高速化（ゴッドメディスンで確認）
・VSYNC:OFF時のウェイトが狂っていたのを修正
・デフォルト設定をVSYNC:OFFからVSYNC:ONに変更

v1.26
・sceKernelLibcClockの折り返し時にフリーズするバグを修正
・コンパイラをgcc4.0.0（psptoolchain）に変更
　（実行されるはずのない部分のコードを追加変更しただけで
　なぜか全体が重くなったりしていたのが、コンパイラ変更で解消した）

v1.25
・ファイルソートの高速化　by ruka
・filesを使用前にゼロクリア　by ruka
・Load State時に即ゲームに戻るように修正
・sgbの拡張子に対応
・MAX_ENTRYを1024から2048に引き上げ
・スリープ復帰処理をより正しく
・設定が変更されたときだけコンフィグ保存するようにした
・書庫内ファイル選択
・VSYNCにsceDisplayWaitVblank()を使うようにした
・ROM削除時にOKとCANCELを選べるようにした
・赤外線入力に仮対応
　（でたらめ。受光部のON/OFF状態を直接読めればあるいは…）
・バッテリーライフ残量を表示（build 2005/06/22）
・CtrlReadをCtrlPeekに変更（build 2005/06/24）
・読み込み時にROM TYPEを表示 （build 2005/06/24）
・lzh対応に関しては消費メモリ量が多いので見送りました
　（GBROMの最大容量は8MB）

v1.24
・オーバークロックオプション追加
・メニュー表示中は222MHzに固定
・メモステアクセス時も222MHzに固定
・ROMフォルダのパスを保存
・メニューの視認性を向上
・ROM削除機能追加
・SAVE THUMBをONからOFFにしてステートセーブすると
　ステートファイルだけ上書きされてサムネイルファイルが
　そのまま残ってしまい、結果として内容とサムネイルが
　食い違ってしまうバグを修正
・ゲーム中のメッセージ表示時間を１秒に固定。以前は60フレーム
　カウントだったので、フレームスキップ具合によって表示時間が変わってた。
・Nega-Posiの名前をReverseに変更
・メニュー表示中にスリープしてから復帰するとスリープフラグが
　立ち、それによりゲーム再開時に不要なウェイトがかかってしまうバグを修正
・カーソルの点滅処理を変更
・sceDisplayIsVblankによるVSYNCの効率化
・使わないスクリーンサイズとGB COLORを無効化できるようにした。
　（ショートカットキーの動作に反映）
・ショートカットキーの入力判定で同時押しを優先的に処理
　（ショートカットキーをビット数順にソート）
・半角フォントの + と - を書き換え
